

name, number, text, ptype, rarity, hp, weakness, resistance, retreatcost, img_src<br>



<% @cards.each do |card| %>
  <%= link_to card.name, "/cards/#{card.id}" %>,
  <%= card.number %>,
  <%= card.text %>,
  <%= card.ptype %>,
  <%= card.rarity %>,
  <%= card.hp %>,
  <%= card.weakness %>,
  <%= card.resistance %>,
  <%= card.retreatcost %>,
  <%= card.img_src %><br>
<% end %>
